<div class="listnewPage">
  <div class="container">
    <%= form_with model: @list, url: { action: :create }, class: "new_list", local: true do |f| %>
      <% if @list.errors.any? %>
        <p class="text-danger">依頼者名は1～255文字以内で入力してください</p>
      <% end %>
      <%= f.label :依頼者 %>
      <%= f.text_field :title, autofocus: true, class: "form-control listName", placeholder: "依頼者名" %>
      <div class="text-center"><%= f.submit "作成", class: "submitBtn" %></div>
    <% end %>
  </div>
</div>